#include<iostream>
using namespace std;
int main()
{
	int m, t, s;
	cin >> m >> t >> s;
	if (t == 0)
	{
		cout << 0;
	}
	else
	{
		if (s % t == 0)
		{
			cout << max(m - s / t, 0);//max(a,b)是输出最大值，a大输出a，b大输出b
		}
		else if (s % t != 0)
		{
			cout << max(m - s / t - 1, 0);
		}
		return 0;
	}
}
