#include<iostream>
#include<climits>
using namespace std;
int main()
{
	int n;
	int min = INT_MAX;
	cin >> n;
	for (int i = 0; i < n; i++)
	{
		int a;
		cin >> a;
		if (min > a)//打擂台
		{
			min = a;
		}
	}
	cout << min;
	return 0;
}
