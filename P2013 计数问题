#include<iostream>
using namespace std;
int main()
{
	int n, x, a, b;
	int cnt = 0;
	cin >> n >> x;
	for (int i = 1; i <= n; i++)
	{
		a = i;//把值固定
		//不为0就保持循环
		while (a != 0)
		{
			b = a % 10;//看个位
			a = a / 10;//看下一位
			if (b == x)
			{
				cnt++;
			}
		}
	}
	cout<<cnt;
	return 0;
}
